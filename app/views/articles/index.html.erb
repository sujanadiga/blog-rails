<h1>List of articles</h1>
<table border="1">
  <thead>
    <tr>
      <th class="article-title">Title</th>
      <th class="article-text">Content</th>
      <th class="article-action">Action</th>
    </tr>
  </thead>
  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td class="article-title"><%= article.title %></td>
        <td class="article-text"><%= article.text %></td>
        <td class="article-action">
          <%= link_to 'Read', article_path(article) %>
          <%= link_to 'Edit', edit_article_path(article), class: 'btn--warning' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<br/>
<%= link_to 'Write a new atricle', new_article_path, class: 'btn' %>
<%-# link_to 'New', controller: 'articles', action: 'new' -%>
